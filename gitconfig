[user]
  name = Mark
  email = mtdoss@gmail.com
[filter "media"]
  required = true
  clean = git media clean %f
  smudge = git media smudge %f
[alias]
  co = checkout

[filter "hawser"]
  clean = git hawser clean %f
  smudge = git hawser smudge %f
  required = true

[push]
  default = tracking

[core]
  editor = vim
	excludesfile = /Users/markdoss/.gitignore_global

[diff]
  external = git_diff_wrapper

[pager]
  diff =
[filter "lfs"]
	clean = git lfs clean %f
	smudge = git lfs smudge %f
	required = true
